angular.module('MainCtrl', []).controller('MainController', function($scope,$rootScope, Auth) {
	// $rootScope.$watch( Auth.isLoggedIn, function ( isLoggedIn ) {
	//     $rootScope.isLoggedIn = isLoggedIn;
	//     $rootScope.currentUser = Auth.currentUser();
	// });
	
});